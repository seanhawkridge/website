
<% if @post.draft? %>
    <%= button_to "Publish", :update_status_admin_post, params: { post_id: @post.id, post_status: :published  } %>
<% else %>
    <%= button_to "Set Draft", :update_status_admin_post, params: { post_id: @post.id, post_status: :draft } %>
<% end %> 

<%= render "form" %>